import React, { Component } from 'react';

class Splash extends Component {
  render() {
    return (
      <section className="splash">
        <header>
          Successful signup, welcome to the platform.
        </header>
      </section>
    );
  }
}

export default Splash;
